# Changelog

## TAMMsupport 0.2.0

- added read_jdf() to read the important info in the `JDF` sheet,
  reformat into something useable in R
- added read_2ac_sheets() to read and format the important info from
  sheets `2A_CU&M`, `2A_CUnmrkd`, `2A_CU&M_H+N`, and `2A_CU&M_N`.

## TAMMsupport 0.1.0

- Initial package creation. Too much to list here.
