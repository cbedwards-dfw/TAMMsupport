# Quick helper function to format vectors that contain text and ERs

Quick helper function to format vectors that contain text and ERs

## Usage

``` r
quick_er_format(x)
```

## Arguments

- x:

  character vector
